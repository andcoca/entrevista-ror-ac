<div class="row mb-4">
  <div class="col-md-8">
    <h1>Edit Book</h1>
  </div>
</div>

<div class="row">
  <div class="col-md-6">
    <%= form_with(model: @book, local: true) do |form| %>
      <% if @book.errors.any? %>
        <div class="alert alert-danger">
          <h4><%= pluralize(@book.errors.count, "error") %> prohibited this book:</h4>
          <ul>
            <% @book.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="mb-3">
        <%= form.label :title, class: "form-label" %>
        <%= form.text_field :title, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :author, class: "form-label" %>
        <%= form.text_field :author, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :genre, class: "form-label" %>
        <%= form.text_field :genre, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :isbn, class: "form-label" %>
        <%= form.text_field :isbn, class: "form-control", disabled: true %>
      </div>

      <div class="mb-3">
        <%= form.label :total_copies, "Total Copies", class: "form-label" %>
        <%= form.number_field :total_copies, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.submit "Update Book", class: "btn btn-warning" %>
        <%= link_to "Cancel", books_path, class: "btn btn-secondary" %>
      </div>
    <% end %>
  </div>
</div>
